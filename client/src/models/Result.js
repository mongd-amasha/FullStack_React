class Result {
  constructor(id, examId, studentId, score, status) {
    this.id = id
    this.examId = examId
    this.studentId = studentId
    this.score = score
    this.status = status
  }
}

export default Result