class Submission {
  constructor(id, examId, studentId, answers, submittedAt) {
    this.id = id
    this.examId = examId
    this.studentId = studentId
    this.answers = answers
    this.submittedAt = submittedAt
  }
}

export default Submission