PORT=5000
CLIENT_URL=http://localhost:5173

# PostgreSQL settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=fullstack_exam_management

# Optional: use this instead of the individual PostgreSQL settings above
# DATABASE_URL=postgresql://postgres:password@localhost:5432/fullstack_exam_management

JWT_SECRET=replace_with_a_secure_secret_later
